{
  "name": "evergreen.js",
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage",
    "clear-jest-cache": "jest --clearCache"
    },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "jest": "^24.5.0",
    "ts-jest": "^24.0.0",
    "typescript": "^3.3.4000"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
